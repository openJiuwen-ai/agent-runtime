# echo 样例部署配置（复制为 .env 后按需修改）
# 监听地址 / 端口
OPENJIUWEN_SERVICE_HOST=0.0.0.0
OPENJIUWEN_SERVICE_PORT=8090
# 协调用 redis（多副本须共享同一实例）
OPENJIUWEN_SERVICE_REDIS_URL=redis://localhost:6379/0
# redis 键命名空间前缀（多副本须一致才能共享状态）
OPENJIUWEN_SERVICE_REDIS_KEY_PREFIX=service
# 服务标题
OPENJIUWEN_SERVICE_TITLE=echo
