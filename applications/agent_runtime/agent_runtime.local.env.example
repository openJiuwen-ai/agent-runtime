# agent-runtime 本地开发环境（fakeredis + fake K8s + SQLite，仅供开发调试）
AGENT_RUNTIME_MODE=local

OPENJIUWEN_SERVICE_HOST=127.0.0.1
OPENJIUWEN_SERVICE_PORT=8091
# local 模式下 REDIS_URL 被忽略（进程内 fakeredis）；前缀按模块固定
OPENJIUWEN_SERVICE_REDIS_URL=redis://127.0.0.1:6379/0
OPENJIUWEN_SERVICE_REQUEST_TIMEOUT_SECONDS=70
